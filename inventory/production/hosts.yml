---
# This engine talks to Kubernetes through kubeconfig/helm on the control
# node — it does not configure remote hosts. See README.md "Почему localhost".
all:
  children:
    platform:
      hosts:
        localhost:
          ansible_connection: local
